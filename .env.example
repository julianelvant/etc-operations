NEXT_PUBLIC_SUPABASE_URL=https://spaibyuwxbmsvfduscxf.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Server-only — required for Storage mirror + full RLS bypass on writes.
# Dashboard → Project Settings → API → service_role (never expose to the browser).
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Protects GET/POST /api/cron/backup (Vercel Cron sends Authorization: Bearer …)
CRON_SECRET=generate-a-long-random-string
# Bootstrap only: seeded into staff_accounts on first login if the table is empty.
# After that, manage users in Admin → Accounts.
ATTENDANCE_USERNAME=desk
ATTENDANCE_PASSWORD=change-me
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-admin
SESSION_SECRET=generate-a-long-random-string
